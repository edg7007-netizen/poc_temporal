services:
  postgres:
    image: 'postgres:16'
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-lending_engine}
      POSTGRES_USER: ${POSTGRES_USER:-lending}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-lending}
    ports:
      - '5432:5432'
